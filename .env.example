EXPO_PUBLIC_API_URL=http://localhost:3000
# Optional for native apps. Only set this if your backend explicitly requires
# an Origin header from Expo/React Native auth requests, and make sure the same
# URL is whitelisted in Better Auth trusted origins.
# EXPO_PUBLIC_AUTH_ORIGIN=http://localhost:8081
